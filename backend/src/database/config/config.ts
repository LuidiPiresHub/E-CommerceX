import { Options } from 'sequelize';

const config: Options = {
  username: 'root',
  password: 'commerce',
  database: 'E_CommerceX_DB',
  host: 'localhost',
  port: 3306,
  dialect: 'mysql',
};

export = config;
